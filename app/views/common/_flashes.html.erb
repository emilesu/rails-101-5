<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert_dismissible alert-<% flash_class(key) %>">
      <button class="close" data-dismiss>x</button>
      <%= value %>
    </div>
  <% end %>
<% end %>
